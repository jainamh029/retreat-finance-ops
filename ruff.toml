line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
# data/build_dataset.py is a single-run pipeline script kept intentionally compact
# (semicolon-joined assignments, tuple-unpack loops). No logic impact.
"data/build_dataset.py" = ["E702", "B007", "B905"]
